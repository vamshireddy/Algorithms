#include<stdio.h>

struct node* nodr_ptr;

void insert_to_headers(node_ptr start);

void find_and_delete_in_headers(node_ptr start,int ele);

void insert(node_ptr start);

void find_and_delete(node_ptr start,int data_to_find);
